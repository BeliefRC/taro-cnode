import { LOGIN_SUCCESS, LOGIN_FAIL } from '../constants/user'

const INITIAL_STATE = {
  accesstoken: 'af770a36-6d8d-4ede-8b8c-48335214b219'
}

export default function user (state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state
  }
}
